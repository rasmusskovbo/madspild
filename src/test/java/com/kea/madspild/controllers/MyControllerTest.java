package com.kea.madspild.controllers;

import com.kea.madspild.models.Contacts;
import org.junit.jupiter.api.Test;
import org.thymeleaf.model.IModel;

import static org.junit.jupiter.api.Assertions.*;
/*
class MyControllerTest {

    @Test
    void createContact_contactIsAdded() {
        MyController controller = new MyController();

        controller.createContact("Testname", "TestCompany", "Test@Email", "Testmsg", IModel);

        assertEquals(1, controller.getContacts().getContacts().size());
    }
}*/